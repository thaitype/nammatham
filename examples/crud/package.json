{
  "name": "crud",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "ts-node bootstrap.ts && tsc -p tsconfig.app.json",
    "build:watch": "nodemon --watch src --ext ts --exec 'npm run build'",
    "start:watch": "nodemon --watch dist --exec 'npm start'",
    "start": "func start",
    "dev": "npx link ../../ && run-p **:watch"
  },
  "author": "Thada Wangthammang",
  "license": "MIT",
  "dependencies": {
    "@azure/functions": "latest",
    "@mildronize/azure-functions": "latest",
    "nodemon": "^2.0.20",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "ts-node": "^10.9.1",
    "tsx": "latest",
    "typescript": "latest"
  }
}
