{
  "name": "nammatham",
  "version": "3.0.0-canary.0",
  "description": "Type-safe Serverless Library for Azure Functions and friends",
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "bin": {
    "nammatham": "dist/cli.js"
  },
  "exports": {
    "./package.json": "./package.json",
    ".": {
      "types": "./dist/main.d.ts",
      "import": "./dist/main.js",
      "default": "./dist/main.js"
    }
  },
  "scripts": {
    "test": "vitest",
    "prepublishOnly": "npm run build",
    "build": "run-s build:*",
    "build:package": "tsup src/main.ts --dts --format esm --external bun,@types/bun",
    "build:cli": "tsup src/cli.ts --dts --format esm --external bun,@types/bun",
    "format": "prettier -w src",
    "lint": "tsc --noEmit && eslint ./src && prettier -c src",
    "lint:fix": "eslint --fix ./src && prettier -w src",
    "dev": "nodemon --watch src --ext ts --exec 'run-s build'"
  },
  "keywords": [
    "azure-functions",
    "azure"
  ],
  "author": "Thada Wangthammang",
  "license": "MIT",
  "dependencies": {
    "colorette": "^2.0.20",
    "debug": "^4.3.4",
    "dotenv": "^16.4.5",
    "esbuild": "^0.20.2",
    "execa": "^8.0.1",
    "invariant": "^2.2.4",
    "lodash.merge": "^4.6.2",
    "pino": "^8.17.1",
    "pino-dev": "^4.0.3",
    "pkg": "^5.8.1",
    "pkg-fetch": "^3.5.2",
    "tiny-invariant": "^1.3.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/thaitype/nammatham.git"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "peerDependencies": {
    "hono": ">=4.0.0"
  },
  "devDependencies": {
    "@types/debug": "^4.1.12",
    "@types/lodash.merge": "^4.6.9",
    "hono": "^4.3.6"
  }
}
